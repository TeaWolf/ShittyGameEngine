// Teawolf

#include "object_load_parameters.h"

ObjectLoadParameters::ObjectLoadParameters(int p_x, int p_y, int p_width, int p_height, const std::string& p_texture_id)
: x(p_x), y(p_y), width(p_width), height(p_height), texture_id(p_texture_id)
{}
